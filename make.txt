# Makefile for UNIX 

# the compiler: gcc for C program, define as g++ for C++ 
CC = gcc 

# the static library archiver 
AR = ar rcs 

# compiler flags: 
# -g enable debugging 
# -Wall turn on all warnings 
# -D__USE_FIXED_PROTOTYPES__ force the compiler to use the correct headers 
# -ansi don’t use GNU extensions, stick to ANSI 
# -std=c99 use newer version 
CFLAGS=-g -Wall -D__USE_FIXED_PROTOTYPES__ -std=c99

# the build target executable: 
TARGET = common_functions 
LIBRARY = libcommon_functions.a 

all: $(TARGET)

$(TARGET): $(LIBRARY) main.c 
	$(CC) $(CFLAGS) -o $(TARGET) main.c $(LIBRARY)

$(LIBRARY): strings.o base64.o
	$(AR) $(LIBRARY) strings.o base64.o

strings.o: strings.c strings.h 
	$(CC) $(CFLAGS) -c strings.c

base64.o: base64.c base64.h
	$(CC) $(CFLAGS) -c base64.c 

clean: 
	$(RM) $(TARGET) $(LIBRARY) strings.o base64.o
